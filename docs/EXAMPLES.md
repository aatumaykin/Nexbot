# Примеры использования Nexbot

В этом разделе вы найдёте практические примеры использования функций Nexbot.

## Cron планировщик

Настройте автоматическое выполнение задач по расписанию.

### Настройка Cron

1. Создайте файл `jobs.json` в вашем workspace:

```bash
# Создать файл jobs.json
cat > ~/.nexbot/jobs.json << 'EOF'
{
  "jobs": [
    {
      "name": "daily-backup",
      "cron": "0 3 * * *",
      "description": "Запустить ежедневный бэкап в 3:00",
      "enabled": true
    },
    {
      "name": "weekly-report",
      "cron": "0 10 * * 1",
      "description": "Создать недельный отчёт по понедельникам в 10:00",
      "enabled": true
    },
    {
      "name": "hourly-check",
      "cron": "15 * * * *",
      "description": "Проверить статус системы каждый час в 15 минут",
      "enabled": true
    }
  ]
}
EOF
```

2. Включите cron в конфигурации:

```bash
# Редактировать ~/.nexbot/config.toml
nano ~/.nexbot/config.toml
```

Добавьте:

```toml
[cron]
enabled = true
timezone = "UTC"
jobs_file = "jobs.json"
```

3. Перезапустите Nexbot:

```bash
nexbot serve
```

### Формат Cron выражений

```
* * * * *
│ │ │ │ │
│ │ │ │ └─── День недели (0-7, 0 и 7 — воскресенье)
│ │ │ └───── Месяц (1-12)
│ │ └─────── День месяца (1-31)
│ └───────── Час (0-23)
└─────────── Минута (0-59)
```

### Полезные Cron выражения

| Выражение | Описание |
|-----------|----------|
| `* * * * *` | Каждую минуту |
| `*/5 * * * *` | Каждые 5 минут |
| `0 * * * *` | Каждый час |
| `0 0 * * *` | Ежедневно в полночь |
| `0 3 * * *` | Ежедневно в 3:00 |
| `0 9 * * 1` | Каждый понедельник в 9:00 |
| `0 */6 * * *` | Каждые 6 часов |
| `30 11 * * 1-5` | В будни в 11:30 |
| `0 0 1 * *` | Первое число каждого месяца |
| `0 0 1 1 *` | Первое января каждого года |

## Subagents

Создавайте независимые агенты для параллельной работы сложных задач.

### Как работает Spawn Tool

**Spawn tool** позволяет LLM создавать изолированных дочерних агентов для выполнения задач.

**Процесс:**
1. **Создание:** `spawn(task)` создаёт новый subagent
2. **Выполнение:** Subagent выполняет задачу с полным доступом ко всем инструментам
3. **Ожидание:** Родительский агент синхронно ожидает результата
4. **Возврат:** Subagent возвращает результат в родительскую сессию
5. **Удаление:** Subagent удаляется из registry и его сессия очищается

**Преимущества:**
- ✅ Изоляция — задачи не влияют друг на друга
- ✅ Параллельность — можно выполнять несколько задач одновременно
- ✅ Автоочистка — subagents удаляются после выполнения
- ✅ Полный доступ — subagents могут использовать spawn (вложенность!)

### Конфигурация

В `~/.nexbot/config.toml`:

```toml
[subagent]
enabled = true
max_concurrent = 10
timeout_seconds = 300
session_prefix = "subagent-"
```

### Примеры использования Subagents

#### Пример 1: Анализ репозитория

**Запрос:**
```
Проанализируй структуру моего проекта.
Создай subagent который:
1. Склонирует репозиторий https://github.com/user/repo.git
2. Найдёт все файлы .go
3. Проанализирует структуру пакетов
4. Вернёт отчёт по структуре
```

**Что делает LLM:**
1. Вызывает `spawn` tool с описанием задачи
2. Ожидает результата

**Что делает Subagent:**
1. Выполняет `git clone`
2. Использует `list_dir` для поиска файлов
3. Использует `read_file` для анализа
4. Возвращает отчёт

**Результат:**
```
Структура проекта:
├── cmd/
│   └── nexbot/
│       └── main.go
├── internal/
│   ├── agent/
│   ├── tools/
│   └── ...
└── README.md

Пакеты: 15, Go файлов: 87
```

#### Пример 2: Мониторинг логов

**Запрос:**
```
Проверь есть ли ошибки в логах.
Создай subagent который:
- Прочитает ~/.nexbot/nexbot.log
- Ищет ошибки за последний час
- Суммирует найденные ошибки
```

**Что происходит:**
1. LLM вызывает `spawn("Прочитай ~/.nexbot/nexbot.log, ищи ERROR за последний час, суммируй")`
2. Subagent читает файл, фильтрует логи, создаёт резюме
3. Subagent возвращает результат в родительскую сессию
4. LLM видит результат и продолжает диалог

**Результат:**
```
Найдено 3 ошибки за последний час:
1. [14:30:15] Failed to connect to database
2. [14:35:42] Timeout waiting for response
3. [14:42:10] Invalid API key format

Рекомендация: Проверьте конфигурацию базы данных и API ключей.
```

#### Пример 3: Пакетная обработка

**Запрос:**
```
Обработай все мои документы.
Создай subagent который:
- Пройдёт по всем файлам в ~/Documents
- Прочитает каждый .txt файл
- Извлечёт ключевую информацию
- Вернёт сводку
```

**Результат:**
```
Обработано 25 файлов:
- Суммарный объём: 1.2 MB
- Средний размер: 48 KB
- Темы: работа (15), финансы (6), заметки (4)

Ключевые моменты:
- В работе.txt: План встреч на следующую неделю
- В финансы.txt: Расходы за месяц: $1,234
```

#### Пример 4: Nested Spawn (вложенные subagents)

Subagent может создавать других subagents!

**Запрос:**
```
Сделай глубокий анализ системы.
Создай subagent который:
1. Проверит состояние системы (CPU, память, диск)
2. Создаст 3 subagents для параллельного анализа:
   - Один для проверки сети
   - Один для проверки сервисов
   - Один для проверки логов
3. Соберёт результаты от всех
4. Вернёт сводный отчёт
```

**Процесс:**
```
Parent Agent
  └─> Subagent A (состояние системы)
       ├─> Subagent B1 (проверка сети)
       ├─> Subagent B2 (проверка сервисов)
       └─> Subagent B3 (проверка логов)
       (все выполняются параллельно)
       └─> Результаты собираются в Subagent A
            └─> Отчёт возвращается в Parent Agent
```

### Логирование Subagents

Все действия subagents логируются:

```bash
# Просмотреть логи создания subagents
grep "subagent spawned" ~/.nexbot/nexbot.log

# Просмотреть логи выполнения задач
grep "processing task in subagent" ~/.nexbot/nexbot.log

# Просмотреть ошибки subagents
grep "subagent processing failed" ~/.nexbot/nexbot.log
```

### Ограничения

- **Max concurrent:** Максимум `max_concurrent` subagents одновременно
- **Timeout:** По умолчанию 300 секунд на задачу
- **Storage:** Сессии хранятся в `~/.nexbot/sessions/subagents/`
- **Cleanup:** Subagents удаляются после выполнения задачи

### Troubleshooting

#### Subagent не создаётся

1. Проверьте конфигурацию:
   ```toml
   [subagent]
   enabled = true
   ```

2. Проверьте логи:
   ```bash
   grep "failed to spawn" ~/.nexbot/nexbot.log
   ```

#### Subagent зависает

1. Проверьте timeout:
   ```toml
   [subagent]
   timeout_seconds = 300
   ```

2. Если задача долгая, увеличьте timeout в spawn call:
   ```
   spawn("Задача", timeout_seconds=600)
   ```

#### Subagent не возвращает результат

1. Проверьте, что subagent использует инструменты для возврата результата
2. Проверьте логи на ошибки выполнения
3. Убедитесь, что задача subagent корректно сформулирована

## HEARTBEAT

Настройте автоматические проверки здоровья системы.

### Создание HEARTBEAT.md

Создайте файл `HEARTBEAT.md` в вашем workspace:

```bash
cat > ~/.nexbot/HEARTBEAT.md << 'EOF'
---
interval: 300
description: Проверка здоровья системы
---

Выполни полную проверку здоровья системы:

1. Проверь статус всех служб
   - Проверь работу SSH
   - Проверь работу HTTP сервера (если есть)
   - Проверь работу базы данных (если есть)

2. Проверь дисковое пространство
   - Проверь свободное место на /
   - Проверь свободное место в /home
   - Предупреди, если свободно < 20%

3. Проверь нагрузку системы
   - CPU использование
   - Память использование
   - I/O wait

4. Проверь последние ошибки
   - Просмотри /var/log/syslog
   - Ищи критические ошибки за последний час
   - Суммируй найденные ошибки

5. Сгенерируй отчёт
   - Общее состояние системы (OK/WARNING/CRITICAL)
   - Детали по каждому пункту
   - Рекомендации по устранению проблем
EOF
```

### Конфигурация HEARTBEAT

HEARTBEAT интегрирован с cron системой. Убедитесь, что cron включён:

```toml
[cron]
enabled = true
timezone = "UTC"
jobs_file = "jobs.json"
```

### Просмотр логов HEARTBEAT

```bash
# Проверить историю выполнения HEARTBEAT
grep "HEARTBEAT" ~/.nexbot/nexbot.log

# Просмотреть последнее выполнение HEARTBEAT
grep -A 20 "HEARTBEAT.*completed" ~/.nexbot/nexbot.log | tail -20
```

### Примеры HEARTBEAT задач

#### Проверка бэкапов

```markdown
---
interval: 86400
description: Проверка бэкапов
---

Проверь состояние бэкапов:
1. Проверь существование последних бэкапов
2. Проверь размер бэкапов
3. Проверь дату последнего бэкапа
4. Сообщи, если бэкапы отсутствуют или старые (>7 дней)
```

#### Проверка SSL сертификатов

```markdown
---
interval: 3600
description: Проверка SSL сертификатов
---

Проверь SSL сертификаты:
1. Проверь срок действия сертификата для example.com
2. Проверь срок действия сертификата для api.example.com
3. Сообщи, если сертификаты истекают в течение 30 дней
```

#### Проверка диска

```markdown
---
interval: 600
description: Проверка дискового пространства
---

Проверь дисковое пространство:
1. Проверь использование /
2. Проверь использование /home
3. Проверь использование /var
4. Сообщи, если использование > 80%
```

## Инструменты работы с файлами

### Чтение файла

```
Прочитай файл ~/.nexbot/config.toml и покажи содержимое
```

### Запись файла

```
Запиши следующий текст в файл test.txt:
```

Это тестовый файл.
```
```

### Добавление в файл

```
Добавь строку "Новая строка" в конец файла test.txt
```

### Список файлов

```
Покажи список файлов в директории ~/Documents
```

```
Покажи список всех .go файлов в директории ./internal/agent/
```

```
Покажи список файлов рекурсивно в директории ~/Projects/
```

## Shell команды

### Безопасные команды

Nexbot по умолчанию разрешает только безопасные команды:

```
Выполни: ls -la
```

```
Выполни: pwd
```

```
Выполни: date
```

```
Выполни: echo "Привет, мир!"
```

### Git операции

После добавления `git` в `allowed_commands`:

```
Покажи статус git репозитория
```

```
Покажи последние 5 коммитов
```

```
Создай новую ветку feature-test
```

### Команды с пайплайнами

```
Выполни: cat file.txt | grep "ключевое слово"
```

### Скрипты

Создайте скрипт и выполните его:

```
Создай скрипт backup.sh который:
- Создаёт бэкап ~/Documents
- Сохраняет в ~/backup/documents-$(date +%Y%m%d).tar.gz
- Делает этот скрипт исполняемым
```

Затем:

```
Выполни: ~/backup.sh
```

## Настройка безопасности

### Ограничение доступа по пользователям

```toml
[channels.telegram]
enabled = true
token = "${TELEGRAM_BOT_TOKEN}"
allowed_users = ["123456789", "987654321"]
```

### Ограничение доступа к директориям

```toml
[tools.file]
enabled = true
whitelist_dirs = ["~/.nexbot", "~/projects", "~/Documents"]
read_only_dirs = ["/etc", "/usr", "/bin"]
```

### Ограничение shell команд

```toml
[tools.shell]
enabled = true
allowed_commands = ["ls", "cat", "grep", "find", "cd", "pwd", "echo", "date", "git"]
```

## Навыки (Skills)

### Создание простого навыка

1. Создайте директорию навыка:

```bash
mkdir -p ~/.nexbot/skills/weather
```

2. Создайте `SKILL.md`:

```markdown
---
name: weather
description: Предоставляет информацию о погоде
tools: [shell_exec]
---

# Weather Skill

Этот навык позволяет получить информацию о погоде.

## Использование

Чтобы получить погоду, попросите:
"Какая погода в Москве?"

Навык использует внешнюю команду для получения данных о погоде.

## Поддерживаемые города

- Москва
- Санкт-Петербург
- Другие крупные города России
```

### Создание навыка для работы с API

```markdown
---
name: github
description: Работа с GitHub API
tools: [shell_exec, read_file, write_file]
---

# GitHub Skill

Навык для работы с GitHub API.

## Возможности

- Получение информации о репозитории
- Создание issues
- Чтение и создание pull requests

## Использование

"Покажи информацию о репозитории user/repo"
"Создай issue с названием 'Bug found'"
```

## Мониторинг и отладка

### Проверка логов

```bash
# Последние 50 строк лога
tail -50 ~/.nexbot/nexbot.log

# Поиск ошибок
grep ERROR ~/.nexbot/nexbot.log

# Поиск по ключевому слову
grep "HEARTBEAT" ~/.nexbot/nexbot.log
```

### Отладка с помощью verbose логирования

В `~/.nexbot/config.toml`:

```toml
[logging]
level = "debug"
format = "text"
output = "stdout"
```

### Тестирование конфигурации

```bash
nexbot config validate
```

### Тестирование LLM провайдера

```bash
nexbot test llm
```

## Полные примеры конфигураций

### Минимальная конфигурация

```toml
[workspace]
path = "~/.nexbot"

[llm]
provider = "zai"

[llm.zai]
api_key = "${ZAI_API_KEY}"
base_url = "https://api.z.ai/api/coding/paas/v4"
model = "glm-4.7-flash"

[channels.telegram]
enabled = true
token = "${TELEGRAM_BOT_TOKEN}"

[logging]
level = "info"
```

### Продвинутая конфигурация

```toml
[workspace]
path = "~/.nexbot"
bootstrap_max_chars = 20000

[agent]
model = "glm-4.7-flash"
max_tokens = 8192
max_iterations = 20
temperature = 0.7
timeout_seconds = 60

[llm]
provider = "zai"

[llm.zai]
api_key = "${ZAI_API_KEY}"
base_url = "https://api.z.ai/api/coding/paas/v4"
timeout_seconds = 60
model = "glm-4.7-flash"

[channels.telegram]
enabled = true
token = "${TELEGRAM_BOT_TOKEN}"
allowed_users = ["123456789"]

[tools.file]
enabled = true
whitelist_dirs = ["~/.nexbot", "~/projects", "~/Documents"]
read_only_dirs = ["/etc", "/usr", "/bin"]

[tools.shell]
enabled = true
allowed_commands = ["ls", "cat", "grep", "find", "cd", "pwd", "echo", "date", "git", "curl"]
working_dir = "~/.nexbot"
timeout_seconds = 30

[cron]
enabled = true
timezone = "UTC"
jobs_file = "jobs.json"

[workers]
pool_size = 5
queue_size = 100

[subagent]
enabled = true
max_concurrent = 10
timeout_seconds = 300
session_prefix = "subagent-"

[logging]
level = "info"
format = "json"
output = "~/.nexbot/nexbot.log"

[message_bus]
capacity = 1000
```

## Устранение неполадок

### Проблема: Бот не отвечает

1. Проверьте, что бот запущен:
   ```bash
   ps aux | grep nexbot
   ```

2. Проверьте логи на ошибки:
   ```bash
   tail -50 ~/.nexbot/nexbot.log
   ```

3. Проверьте конфигурацию:
   ```bash
   nexbot config validate
   ```

4. Проверьте подключение к Telegram:
   ```bash
   curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
   ```

### Проблема: Subagent не создаётся

1. Проверьте, что subagent включён:
   ```toml
   [subagent]
   enabled = true
   ```

2. Проверьте лимиты:
   ```toml
   [subagent]
   max_concurrent = 10
   ```

3. Проверьте логи на ошибки создания subagent

### Проблема: Cron задачи не выполняются

1. Проверьте, что cron включён:
   ```toml
   [cron]
   enabled = true
   ```

2. Проверьте файл jobs.json:
   ```bash
   cat ~/.nexbot/jobs.json
   ```

3. Проверьте правильность cron выражений

4. Проверьте логи выполнения cron:
   ```bash
   grep "cron" ~/.nexbot/nexbot.log
   ```

## Дополнительные ресурсы

- [Конфигурация](CONFIGURATION.md) — полная справка по конфигурации
- [Архитектура](ARCHITECTURE.md) — архитектура системы
- [Разработка](DEVELOPMENT.md) — руководство для разработчиков
- [Вводный гайд](README.md) — быстрый старт

---

Happy coding with Nexbot! 🚀
