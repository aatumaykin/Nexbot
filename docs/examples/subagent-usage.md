# Примеры использования Subagent Spawn Tool

В этом документе показаны практические примеры использования spawn tool в Nexbot.

## Базовый пример

### Описание задачи

Пользователь просит агента проанализировать последние изменения в проекте.

### Диалог с агентом

```
Вы: Проанализируй последние 10 коммитов в репозитории и создай резюме

Агент: Хорошо, создам subagent для этой задачи.
[spawn: "Прочитай git log -10 --oneline, проанализируй каждое сообщение,
        сгруппируй по типам изменений (feat, fix, docs, chore),
        создай краткое резюме"]

Создан subagent с ID: a1b2c3d4-e5f6-7890-abcd-ef1234567890

[Subagent выполняет задачи...]
$ git log -10 --oneline
abc123 feat: Add spawn tool implementation
def456 fix: Fix memory leak in subagent manager
...

[Subagent анализирует и возвращает результат]

Агент: Вот анализ последних 10 коммитов:

**Краткое резюме:**
- Всего: 10 коммитов
- Новые функции (feat): 4
- Исправления (fix): 3
- Документация (docs): 2
- Технические задачи (chore): 1

**Детали:**
1. feat: Add spawn tool implementation — реализация инструмента spawn
2. feat: Add subagent manager — добавление менеджера subagents
...
```

## Продвинутый пример

### Анализ системных логов с несколькими subagents

**Сценарий:**

Пользователь хочет проверить состояние системы через несколько независимых проверок.

### Диалог

```
Вы: Проверь состояние сервера:
- Статус системы (CPU, память, диск)
- Состояние служб (nginx, postgres, redis)
- Ошибки в логах за последний час
- Состояние сети

Агент: Создам несколько subagents для параллельной проверки.
[spawn: "Проверь CPU и память через /proc/loadavg и free"]
[spawn: "Проверь статус служб через systemctl status"]
[spawn: "Прочитай /var/log/syslog, ищи ERROR за последний час"]
[spawn: "Проверь сеть через ping google.com и traceroute"]

[Все 4 subagents созданы и выполняются параллельно]

[Результаты собираются...]

Агент: Готово! Вот отчёт по состоянию сервера:

## 📊 Система

**CPU:**
- Load average: 0.45, 0.52, 0.48 (за 1, 5, 15 минут)
- Использование: 45%
- Статус: ✅ OK

**Память:**
- Всего: 16 GB
- Использовано: 8.2 GB (51%)
- Доступно: 7.8 GB
- Статус: ✅ OK

**Диск:**
- /dev/sda1: 45% использовано (450 GB из 1 TB)
- Статус: ✅ OK

## 🚀 Службы

**nginx:**
- Статус: ✅ Running
- Uptime: 15 дней
- Ответ на запрос: OK

**postgres:**
- Статус: ✅ Running
- Uptime: 15 дней
- Connections: 45 активных

**redis:**
- Статус: ✅ Running
- Uptime: 15 дней
- Memory: 256 MB

## 📝 Логи

**Ошибки за последний час:**
1. [14:30:15] Connection timeout to external API
2. [14:42:30] Rate limit exceeded for requests
3. [15:00:00] Health check passed

**Вывод:** 3 предупреждения, критических ошибок нет

## 🌐 Сеть

**Пинг до google.com:**
- Среднее время: 12ms
- Потерь: 0%
- Статус: ✅ OK

**Вывод:** Сервер работает стабильно, небольшие проблемы с внешним API
```

## Вложенные subagents

### Параллельная обработка файлов

**Сценарий:**

Обработка большого количества файлов с использованием nested spawn.

### Диалог

```
Вы: Обработай все проекты в ~/projects

Агент: Создам main subagent для координации.
[spawn: "Пройди по ~/projects, для каждого проекта создай отдельный subagent
        который проанализирует структуру и найдёт README.md,
        соберёт результаты в сводный отчёт"]

[Main Subagent создаёт 10 subagents, по одному на проект]
[Все subagents выполняются параллельно]

[Subagent 1: проект1]
- Прочитан README.md
- Найдено 5 файлов Python
- Статус: Активная разработка

[Subagent 2: проект2]
- Прочитан README.md
- Найдено 12 файлов Go
- Статус: Документация завершена

... (параллельно)

[Main Subagent собирает результаты]

Агент: Обработка завершена! Обработано 10 проектов:

## Сводка по проектам

| Проект   | Язык       | Статус                 | Файлов |
| -------- | ---------- | ---------------------- | ------ |
| project1 | Python     | Активная разработка    | 5      |
| project2 | Go         | Документация завершена | 12     |
| project3 | JavaScript | В планах               | 8      |
| ...      | ...        | ...                    | ...    |

**Итого:**
- Проектов: 10
- Активные: 4
- Завершённые: 3
- В планах: 3
```

## Практические советы

### 1. Правильная формулировка задач

**Плохо:**
```
Сделай что-то с файлами
```

**Хорошо:**
```
Создай subagent который:
1. Пройдёт по директории ~/Documents
2. Найдёт все .txt файлы
3. Прочитает каждый файл
4. Извлечёт даты и суммы
5. Вернёт сводный отчёт
```

### 2. Оптимизация с nested spawn

Если нужно выполнить много независимых задач:

**Медленно:**
```
Создай subagent который:
1. Проверит сайт A
2. Проверит сайт B
3. Проверит сайт C
(последовательно)
```

**Быстро:**
```
Создай subagent который:
1. Создаст 3 subagents для параллельной проверки
   - Один для сайта A
   - Один для сайта B
   - Один для сайта C
2. Соберёт результаты
3. Вернёт отчёт
(параллельно)
```

### 3. Обработка ошибок

Subagent может возвращать ошибки:

```
Вы: Проверь конфигурацию сервера

Агент: Создам subagent для проверки.
[spawn: "Прочитай /etc/nginx/nginx.conf, проверь валидность"]

[Subagent возвращает ошибку]

Агент: Произошла ошибка при проверке конфигурации:
❌ Permission denied: /etc/nginx/nginx.conf

Рекомендация: Запустите с правами sudo или проверьте права доступа.
```

### 4. Мониторинг выполнения

**Просмотр логов:**
```bash
# Созданные subagents
grep "subagent spawned" ~/.nexbot/nexbot.log

# Выполненные задачи
grep "task processed in subagent" ~/.nexbot/nexbot.log

# Ошибки
grep "subagent processing failed" ~/.nexbot/nexbot.log
```

**Лог в реальном времени:**
```bash
tail -f ~/.nexbot/nexbot.log | grep subagent
```

## Тестирование spawn tool

### Простой тест

```
Вы: Создай subagent который просто скажет "Hello, world!" и вернёт это сообщение

Агент: [spawn: "Верни текст 'Hello, world!'"]

[Subagent выполняет задачу]

Агент: Вот результат:
✅ Hello, world!
```

### Тест с вложенностью

```
Вы: Сделай 3 вложенных subagents:
1. Первый создаст 2 под-subagents
2. Второй просто вернёт "Task 2"
3. Третий вернёт "Task 3"

Агент: Отлично, сделаю это.
[spawn: "Создай 2 под-subagents и собери их результаты"]
[spawn: "Верни 'Task 2'"]
[spawn: "Верни 'Task 3'"]

Агент: Все подзадачи выполнены:
- Вложенные: ✅ Результаты собраны
- Task 2: ✅ Выполнено
- Task 3: ✅ Выполнено
```

### Тест с ошибками

```
Вы: Попробуй выполнить задачу с ошибкой

Агент: Попробую.
[spawn: "Выполни команду 'cat /nonexistent/file.txt'"]

[Subagent возвращает ошибку]

Агент: Произошла ошибка при выполнении:
❌ cat: /nonexistent/file.txt: No such file or directory

Рекомендация: Убедитесь, что файл существует перед его чтением.
```
