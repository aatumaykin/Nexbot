# Nexbot — Документация

## О проекте

**Nexbot** — легковесный self-hosted ИИ-агент на Go для управления задачами через Telegram с LLM-провайдером Z.ai (GLM-4.7) и системой навыков (skills).

## Ключевые возможности

- 🤖 **Ультралёгкий** (~8-10K строк кода)
- 🔌 **Интеграция с Telegram** — общение через бота
- 🧠 **Z.ai LLM** — GLM-4.7 Flash для быстрых ответов
- 🛠️ **Tool calling** — встроенные инструменты (файлы, shell)
- 📚 **Skills system** — расширяемые навыки (OpenClaw совместимые)
- 💾 **Workspace** — файлы контекста (IDENTITY.md, AGENTS.md, SOUL.md, USER.md, TOOLS.md)
- 📝 **Управление сессиями** — история диалогов
- 🚌 **Message bus** — декомпозированная архитектура
- ⏰ **Cron scheduling** — планирование задач по расписанию
- 🧬 **Subagent spawning** — создание дочерних агентов
- 🔄 **Background tasks** — async worker pool
- 💓 **HEARTBEAT** — система health check
- 🚀 **Single binary** — Linux/macOS/Windows

## Быстрый старт за 5 минут

### 1. Установка (2 минуты)

#### Вариант A: Загрузка бинарника (быстрее всего)

```bash
# Загрузить последний релиз для вашей платформы
curl -L https://github.com/aatumaykin/nexbot/releases/latest/download/nexbot-$(uname -s)-$(uname -m) -o nexbot

# Или вручную с:
# https://github.com/aatumaykin/nexbot/releases

# Сделать исполняемым
chmod +x nexbot

# Переместить в PATH
sudo mv nexbot /usr/local/bin/
```

#### Вариант B: Сборка из исходников

```bash
# Клонировать репозиторий
git clone https://github.com/aatumaykin/nexbot.git
cd nexbot

# Собрать
make build

# Установить
make install
```

### 2. Получение API ключей (2 минуты)

#### Получить Z.ai API ключ

1. Перейти на [Z.ai](https://z.ai)
2. Войти/зарегистрироваться
3. Перейти в раздел API Keys
4. Создать новый API ключ
5. Скопировать ключ (начинается с `zai-` или `sk-`)

#### Получить токен Telegram бота

1. Открыть Telegram и найти [@BotFather](https://t.me/BotFather)
2. Отправить команду `/newbot`
3. Следовать инструкциям:
   - Выбрать имя бота (например, "My Nexbot")
   - Выбрать username (например, "my_nexbot_bot")
4. BotFather даст токен (например, `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Скопировать токен

### 3. Настройка (30 секунд)

```bash
# Создать директорию конфигурации
mkdir -p ~/.nexbot

# Создать файл конфигурации
cat > ~/.nexbot/config.toml << 'EOF'
[workspace]
path = "~/.nexbot"

[llm]
provider = "zai"

[llm.zai]
api_key = "ВАШ_ZAI_API_КЛЮЧ_ЗДЕСЬ"
base_url = "https://api.z.ai/api/coding/paas/v4"
model = "glm-4.7-flash"

[channels.telegram]
enabled = true
token = "ВАШ_TELEGRAM_BOT_TOKEN_ЗДЕСЬ"
allowed_users = []

[logging]
level = "info"
format = "json"
output = "stdout"
EOF

# Замените на реальные ключи
nano ~/.nexbot/config.toml
```

Или используйте переменные окружения (более безопасно):

```bash
cat > ~/.nexbot/config.toml << 'EOF'
[workspace]
path = "~/.nexbot"

[llm]
provider = "zai"

[llm.zai]
api_key = "${ZAI_API_KEY}"
base_url = "https://api.z.ai/api/coding/paas/v4"
model = "glm-4.7-flash"

[channels.telegram]
enabled = true
token = "${TELEGRAM_BOT_TOKEN}"
allowed_users = []
EOF

# Установить переменные окружения
export ZAI_API_KEY="zai-ваш-api-ключ-здесь"
export TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"

# Добавить в ~/.bashrc или ~/.zshrc для постоянного использования
echo 'export ZAI_API_KEY="zai-ваш-api-ключ-здесь"' >> ~/.bashrc
echo 'export TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"' >> ~/.bashrc
source ~/.bashrc
```

### 4. Проверка конфигурации (10 секунд)

```bash
# Проверить конфигурацию
nexbot config validate

# Должно показать: ✅ Configuration is valid
```

Если проверка не прошла:
- Проверьте формат API ключа (должен начинаться с `zai-` или `sk-`)
- Проверьте формат токена Telegram (должен быть `bot_id:token`)
- Убедитесь, что нет пробелов в конце значений

### 5. Запуск (10 секунд)

```bash
# Запустить Nexbot
nexbot serve
```

Вы должны увидеть вывод:

```
✅ Configuration loaded
✅ Logger initialized
✅ Message bus started
✅ Telegram bot initialized (bot_id: 1234567890, username: my_nexbot_bot)
✅ Inbound message queue subscribed
✅ Outbound message queue subscribed
✅ Nexbot is running
```

### 6. Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота (username, который вы выбрали)
3. Начните чат
4. Отправьте сообщение: "Привет, кто ты?"
5. Бот должен ответить!

## Документация

- **[Конфигурация](KONFIGURACIYA.md)** — полная справка по настройкам
- **[Архитектура](ARHITEKTURA.md)** — устройство системы и компоненты
- **[Разработка](RAZRABOTKA.md)** — руководство для разработчиков
- **[Примеры](PRIMERY.md)** — практические примеры использования

## CLI команды

```bash
nexbot serve              # Запустить Nexbot агент (основная команда)
nexbot run                # Запустить Nexbot агент
nexbot config validate    # Проверить файл конфигурации
nexbot test               # Проверить компоненты Nexbot
nexbot version            # Показать версию
nexbot --help             # Показать справку
nexbot --version          # Показать версию
```

## Структура проекта

```
cmd/nexbot/          — точка входа
internal/
  agent/             — основная логика агента (loop, context, session, memory, tools)
  bus/               — message bus (очередь, события)
  channels/          — коннекторы (telegram)
  config/            — конфигурация
  llm/               — LLM провайдеры (zai, mock)
  logger/            — логирование
  skills/            — система навыков
  tools/             — инструменты (file, shell)
  workspace/         — управление workspace
pkg/                  — экспортируемые пакеты
skills/               — внешние навыки (OpenClaw совместимые)
workspace/            — bootstrap файлы (~/.nexbot/)
```

## Устранение неполадок

### Проблема: "telegram token is required"
- **Решение:** Убедитесь, что заменили `ВАШ_TELEGRAM_BOT_TOKEN_ЗДЕСЬ` в конфиге

### Проблема: "Z.ai API key is required"
- **Решение:** Убедитесь, что заменили `ВАШ_ZAI_API_КЛЮЧ_ЗДЕСЬ` в конфиге

### Проблема: "Authentication error"
- **Решение:** Проверьте, что API ключ правильный и не истёк

### Проблема: Бот не отвечает
- **Решение:**
  - Проверьте, что бот включён в конфиге: `channels.telegram.enabled = true`
  - Проверьте, что токен Telegram правильный
  - Проверьте логи на наличие ошибок

### Проблема: "Permission denied" при установке
- **Решение:** Используйте `sudo make install` или установите в `~/bin`: `make install-user`

## Следующие шаги

Теперь, когда Nexbot запущен:

1. **Создайте bootstrap файлы** в `~/.nexbot/`:
   - `IDENTITY.md` — основная идентичность бота
   - `AGENTS.md` — инструкции агента
   - `SOUL.md` — личность бота
   - `USER.md` — ваш профиль

2. **Создайте навыки** в `~/.nexbot/skills/`:
   - Каждый навык — это файл `SKILL.md` с YAML frontmatter
   - Обучите бота новым возможностям

3. **Настройте разрешённых пользователей** для безопасности:
   ```toml
   [channels.telegram]
   allowed_users = ["123456789", "987654321"]  # Разрешить только конкретных пользователей
   ```

4. **Изучите дополнительные возможности:**
   - См. `config.example.toml` для всех опций
   - Прочтите [Конфигурация](KONFIGURACIYA.md) для подробной справки
   - Попробуйте [Cron scheduling](PRIMERY.md#cron)
   - Попробуйте [Subagent spawning](PRIMERY.md#subagenty)
   - Попробуйте [HEARTBEAT задачи](PRIMERY.md#serdecebeat)

## Версия Go

Требуется **Go 1.25.5+**

## Лицензия

MIT License — см. [LICENSE](../LICENSE) для подробностей

---

**Вдохновлено:** [nanobot](https://github.com/HKUDS/nanobot) и [Nexflow](https://github.com/aatumaykin/nexflow)
