# Nexbot — Ultra-Lightweight Personal AI Agent

**Nexbot** — self-hosted ИИ-агент на Go для управления цифровыми потоками задач через Telegram с LLM-провайдером Z.ai (GLM-4.7) и системой навыков (skills).

## Возможности

- 🔌 **Интеграция с Telegram** — общение через бота
- 🧠 **Z.ai LLM** — GLM-4.7 Flash для быстрых ответов
- 🛠️ **Tool calling** — встроенные инструменты (файлы, shell)
- 📚 **Skills system** — расширяемые навыки (OpenClaw совместимые)
- 💾 **Workspace** — файлы контекста (IDENTITY.md, AGENTS.md, SOUL.md, USER.md, TOOLS.md)
- 📝 **Управление сессиями** — история диалогов
- 🚌 **Message bus** — декомпозированная архитектура
- ⏰ **Cron scheduling** — планирование задач по расписанию
- 🧬 **Subagent spawning** — создание дочерних агентов
- 🔄 **Background tasks** — async worker pool
- 💓 **HEARTBEAT** — система health check
- 🚀 **Single binary** — Linux/macOS/Windows

## Быстрый старт

```bash
# Клонировать репозиторий
git clone https://github.com/aatumaykin/nexbot.git
cd nexbot

# Собрать
make build

# Скопировать и настроить конфигурацию
cp config.example.toml config.toml
# Отредактируйте config.toml и добавьте ваши API ключи

# Запустить
./nexbot serve
```

Подробная инструкция: [docs/README.md](docs/README.md)

## Subagents (дочерние агенты)

Nexbot поддерживает создание дочерних агентов для параллельного выполнения задач. LLM может использовать инструмент `spawn` для создания изолированных subagent-сессий.

**Возможности:**
- Создание subagents с уникальными ID
- Синхронное выполнение задач с ожиданием результата
- Автоматическое удаление subagent после выполнения
- Subagent могут использовать все инструменты (включая spawn для вложенных задач!)
- Изолированные сессии и память
- Graceful shutdown всех subagents

**Как это работает:**

1. **Создание:** LLM вызывает инструмент `spawn("Задача")`
2. **Выполнение:** Создаётся изолированный subagent, который выполняет задачу
3. **Ожидание:** Родительский агент ожидает результата работы subagent
4. **Возврат результата:** Subagent возвращает результат в родительскую сессию
5. **Удаление:** Subagent удаляется из registry и его сессия очищается
6. **Продолжение:** LLM видит результат и продолжает диалог с пользователем

**Пример диалога:**

```
Пользователь: Проанализируй последние 5 коммитов в репозитории

LLM: [вызывает spawn tool]
{
  "task": "Прочитай последние 5 коммитов из git лога,
          проанализируй изменения,
          создай резюме"
}

[Система создаёт subagent]
[Subagent выполняет команду: git log -5 --oneline]
[Subagent анализирует изменения]
[Subagent возвращает результат]

LLM: Анализ завершён. Вот резюме изменений:
1. feat: Добавлена новая функция X
2. fix: Исправлен баг в модуле Y
...
```

**Конфигурация:**

```toml
[subagent]
enabled = true               # Включить функциональность
max_concurrent = 10          # Максимум одновременных
timeout_seconds = 300        # Таймаут по умолчанию
session_prefix = "subagent-"  # Префикс session ID
```

**Подробнее:** [Примеры использования](docs/EXAMPLES.md#subagents)

## HEARTBEAT (система health check)

HEARTBEAT.md позволяет задать proactive задачи, которые будут выполняться автоматически. Задачи могут быть запланированы через cron-выражения.

**Файл:** `~/.nexbot/HEARTBEAT.md`

**Пример HEARTBEAT.md:**
```markdown
# Daily Health Checks

- Task: "Check system status"
  Schedule: "0 9 * * *"
  Description: "Perform daily health check"

- Task: "Generate daily report"
  Schedule: "0 18 * * *"
  Description: "Generate and send daily report"

- Task: "Cleanup logs"
  Schedule: "0 2 * * 0"
  Description: "Clean up old log files"
```

**Формат:**
- Каждая задача начинается с `- Task: "<описание>"`
- `Schedule` — cron-выражение (мин, час, день месяца, месяц, день недели)
- `tool` — что выполнить (send_message или agent)
- `payload` — параметры для tool (JSON строка)
- `session_id` — куда отправить (только для send_message/agent)
- `Description` — описание задачи

**Примечание:** Для выполнения HEARTBEAT задач необходимо запустить `nexbot serve`.

### Конфигурация

```toml
[heartbeat]
enabled = true
check_interval_minutes = 10
```

### Доступные tools для Heartbeat

- `send_message` — отправить сообщение в канал
- `read_file` — прочитать файл
- `write_file` — записать файл
- `cron` — управление cron задачами



## Конфигурация

См. `config.example.toml` для всех опций конфигурации.

```toml
[agent]
model = "glm-4.7-flash"
max_tokens = 8192

[llm.zai]
api_key = "${ZAI_API_KEY:}"

[channels.telegram]
token = "${TELEGRAM_BOT_TOKEN:}"
allowed_users = []
```

Полная документация: [docs/CONFIGURATION.md](docs/CONFIGURATION.md)

## CLI команды

```bash
# Основные команды
nexbot serve              # Запустить Nexbot агент (основная команда)
nexbot config validate    # Проверить конфигурацию
nexbot test               # Проверить компоненты Nexbot
nexbot --help             # Показать справку

# Cron команды (планирование задач)
nexbot cron add <schedule> <command>  # Добавить запланированную задачу
nexbot cron list                       # Список всех запланированных задач
nexbot cron remove <job_id>           # Удалить запланированную задачу
```

### Примеры использования Cron

```bash
# Добавить задачу, выполняющуюся каждый час
nexbot cron add "0 * * * *" "Run daily report"

# Добавить задачу, выполняющуюся каждый день в 9:00
nexbot cron add "0 9 * * *" "Morning health check"

# Добавить задачу, выполняющуюся каждые 5 минут
nexbot cron add "*/5 * * * *" "Check system status"

# Список всех задач
nexbot cron list

# Удалить задачу по ID
nexbot cron remove job_12345
```

**Примечание:** Пользовательские запросы типа "напомни через X минут" обрабатываются через cron tool с параметрами `tool`, `payload`, `session_id` (обязательные поля). Устаревший параметр `command` больше не поддерживается.

## Сборка

```bash
make build      # Собрать для текущей платформы
make build-all  # Собрать для всех платформ
make test       # Запустить тесты
make install    # Установить в /usr/local/bin
```

## Документация

- [Вводный гайд](docs/README.md) — обзор проекта, установка, быстрый старт
- [Конфигурация](docs/CONFIGURATION.md) — полная справка по настройкам
- [Архитектура](docs/ARCHITECTURE.md) — устройство системы
- [Разработка](docs/DEVELOPMENT.md) — гайд для разработчиков
- [Примеры](docs/EXAMPLES.md) — практические примеры использования
- [Список изменений](CHANGELOG.md) — история версий и изменений

## Лицензия

MIT License — см. [LICENSE](LICENSE) для подробностей.

---

**Вдохновлено:** [nanobot](https://github.com/HKUDS/nanobot) и [Nexflow](https://github.com/aatumaykin/nexflow)
