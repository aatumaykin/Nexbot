package constants

// CommandNewSession is the command to create a new agent session.
const CommandNewSession = "new_session"

// CommandStatus is the command to check the current agent status.
const CommandStatus = "status"

// CommandRestart is the command to restart the current agent session.
const CommandRestart = "restart"
