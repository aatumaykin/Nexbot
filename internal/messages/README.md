# Messages

## Назначение

Messages обеспечивает форматирование и отправку сообщений статуса. Поддерживает красивый вывод информации о сессиях и конфигурации.

## Основные компоненты

### FormatStatusMessage
Форматирует сообщение статуса сессии.

## Использование

### Форматирование статуса

```go
import (
    "github.com/aatumaykin/nexbot/internal/messages"
)

// Форматирование сообщения статуса
statusMsg := messages.FormatStatusMessage(
    sessionID,
    messageCount,
    fileSizeHuman,
    model,
    temperature,
    maxTokens,
)

fmt.Println(statusMsg)
```

### Структура сообщения

```
📊 Session Status
━━━━━━━━━━━━━━━━━━
Session ID: user123-session-456
Messages: 15
Size: 1.2 MB

⚙️ LLM Configuration
━━━━━━━━━━━━━━━━━━
Model: glm-4.7-flash
Temperature: 0.7
Max Tokens: 4096
```

## Конфигурация

### Параметры FormatStatusMessage

- `sessionID` — уникальный ID сессии
- `msgCount` — количество сообщений в сессии
- `fileSizeHuman` — размер файла в человекочитаемом формате
- `model` — имя модели LLM
- `temperature` — температура сэмплирования
- `maxTokens` — максимальное количество токенов

## Зависимости

- `github.com/aatumaykin/nexbot/internal/constants` — константы сообщений

## Примечания

- Размер файла форматируется в человеческом формате (B, KB, MB, GB)
- Сообщение включает header с иконкой и разделители

## См. также

- `internal/commands` — использование в обработчике статуса
